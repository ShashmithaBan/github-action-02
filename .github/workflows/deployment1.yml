name: Deploy project jobs at once

on: push

jobs:

  Deploy:

    runs-on: ubuntu-latest

    steps:
      - name: Get Code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: npm install

      - name:  Run Lint 
        run: npm run lint 

      - name:  Run tests
        run: npm run test 

      - name:  Run Build
        run: npm run Build

      - name: Deploy
        run: echo "Deploy Success"  